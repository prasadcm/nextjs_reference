export type SearchItemRaw = {
    productIcon: string;
    productUrl: string;
    searchText: string;
    searchCount: number;
};

export type SearchItem = {
    product: string;
    productIcon: string;
    productUrl: string;
    searchCount: number;
};
